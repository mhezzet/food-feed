import styled from 'styled-components/native'
import { colors } from '../../constants/Colors'

export const MontserratText = styled.Text`
  font-family: 'Montserrat-400';
  color: ${colors.black};
  font-size: 12px;
`
